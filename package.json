{
  "name": "task-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
      "start": "concurrently \"npm run dev --prefix clientside\" \"npm run dev --prefix server\"",
      "server": "nodemon server.js --prefix server",
      "clientside": "react-scripts start --prefix clientside"
  },
  "dependencies": {
      "concurrently": "^9.0.1"
  }
}
