{
  "name": "task-manager",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node server.js",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client"
  },
  "dependencies": {
      "concurrently": "^9.0.1"
  }
}
